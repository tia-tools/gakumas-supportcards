{
  "name": "gakumas-supportcards",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "bun scripts/generate-taxonomy.ts && bun scripts/generate-cards.ts",
    "test": "bun test",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "@types/js-yaml": "^4.0.9",
    "js-yaml": "^5.4.2",
    "typescript": "^7.0.2"
  }
}
