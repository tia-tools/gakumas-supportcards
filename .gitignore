# dependencies (bun install)
node_modules

# build output
dist

# local cache of upstream gakumasu-diff YAML tables (re-fetched on demand)
.cache

# logs and caches
*.log
.eslintcache
*.tsbuildinfo

# OS / IDE
.DS_Store
.idea

# image extraction (Milestone 4): third-party checkout, virtualenv and downloaded art are never committed
scripts/images/vendor
scripts/images/out
scripts/images/.venv

# wrangler working directory
.wrangler

# python bytecode
__pycache__
